name = "second-brain-mcp"
main = "server.py"
compatibility_date = "2024-03-20"
compatibility_flags = ["python_workers"]

[vars]
# Add non-sensitive vars here or set via `wrangler secret put DATABASE_URL`
